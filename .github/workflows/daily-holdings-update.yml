name: 📊 每日持仓数据更新

on:
  schedule:
    # 每天 UTC 01:00 运行（北京时间上午9点）
    - cron: '0 1 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  update-holdings:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install beautifulsoup4 requests

      - name: 🔍 Show environment info
        run: |
          echo "Python version: $(python --version)"
          echo "Working directory: $(pwd)"
          echo "Git branch: ${{ github.ref_name }}"
          date

      - name: 📊 抓取持仓数据
        run: |
          echo "📅 $(date '+%Y-%m-%d %H:%M:%S')"
          echo "🔍 开始抓取持仓数据..."
          python scripts/scrape_holdings.py || echo "⚠️ 持仓数据抓取脚本不存在"
          echo "✅ 持仓数据抓取完成"

      - name: 📝 创建更新摘要
        run: |
          echo "## 📊 每日持仓数据更新" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🕐 更新时间：$(date '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ 完成的任务" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 持仓数据抓取" >> $GITHUB_STEP_SUMMARY
          echo "- ℹ️ 绩效数据未更新（每2周计算一次）" >> $GITHUB_STEP_SUMMARY

      - name: 🚀 提交并推送
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          if git diff --quiet && git diff --staged --quiet; then
            echo "ℹ️ 没有检测到数据变更"
          else
            echo "📝 提交持仓数据更新..."
            git add data/holdings.json src/holdingsData.ts src/performanceData.json || true
            git commit -m "📊 每日持仓更新: $(date '+%Y-%m-%d')

            - 持仓数据已更新
            - 绩效数据未变更（每2周更新）

            [skip ci]" || echo "ℹ️ 没有新数据需要提交"
            git push
            echo "✅ 持仓数据已提交"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🌐 触发 Netlify 部署
        if: success()
        run: |
          echo "🚀 触发 Netlify 重新部署..."
          response=$(curl -s -X POST -d "{}" "${{ secrets.NETLIFY_BUILD_HOOK }}")
          echo "Netlify 响应: $response"
          echo "✅ Netlify 部署已触发"
        env:
          NETLIFY_BUILD_HOOK: ${{ secrets.NETLIFY_BUILD_HOOK }}
